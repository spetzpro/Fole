# Server Configuration
PORT=3000

# Developer Safety Overrides
# Set to "1" or "true" to allow force-deploying invalid configurations
FOLE_DEV_FORCE_INVALID_CONFIG=0

# Set to "1" or "true" to unlock Developer Mode via localhost overrides
FOLE_DEV_ALLOW_MODE_OVERRIDES=0
