# Module: core.ui.AppShell

## 1. Purpose
Top-level React application shell that renders login, project list, or workspace based on auth state and route.

## 2. Public API
~~~ts
import type { FC } from "react";

export const AppShell: FC = () => JSX.Element;
~~~
