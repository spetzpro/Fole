# Missing Spec Paths Checklist

## Core Layer

- [ ] **core.runtime** — add Spec Path to its block/module/lib spec
- [ ] **core.moduleStateRepository** — add Spec Path to its block/module/lib spec
- [ ] **core.accessControl** — add Spec Path to its block/module/lib spec

## Feature Layer

- [ ] **feature.sketch** — add Spec Path to its block/module/lib spec
- [ ] **feature.files** — add Spec Path to its block/module/lib spec
- [ ] **feature.comments** — add Spec Path to its block/module/lib spec
- [ ] **feature.measure** — add Spec Path to its block/module/lib spec

## Library / Technical Modules

- [ ] **lib.image** — add Spec Path to its block/module/lib spec
- [ ] **lib.geo** — add Spec Path to its block/module/lib spec
- [ ] **lib.jobs** — add Spec Path to its block/module/lib spec
- [ ] **lib.diagnostics** — add Spec Path to its block/module/lib spec
